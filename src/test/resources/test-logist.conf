# config used for the unit tests
logist{
  ignore {
    info {
      OsgiInstallerImpl = ["*"]
      "*" = ["org.apache.sling.audit.osgi.installer"]
    }
  }
  output {
    info = "info"
    debug = "info"
  }
}